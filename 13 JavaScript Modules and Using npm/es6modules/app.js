import { uniq } from 'lodash';
import insane from 'insane';
import jsonp from 'jsonp';
import { apiKey as key, url, sayHi, old, dog } from './src/config';

console.log(Key, url);

sayHi('Nando');

const ages = [27, 6, 97, 26, 1, 98];
console.log(uniq(ages));